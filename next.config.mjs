/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'media.licdn.com',
      'i.postimg.cc',
      'encrypted-tbn0.gstatic.com',
      'images.unsplash.com'
    ],
  },
};

export default nextConfig;
